from setuptools import find_packages, setup

setup(
    name='tprob',
    packages=find_packages(include=['tprob']),
    version='0.1.0',
    description='',
    author='Me',
    license='MIT',
    install_requires=[
        'requests'
    ]
)